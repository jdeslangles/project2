<%= form_for [@album.user, @album, @photo] do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %><br />
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %><br />
  </div>
  <div class="field">
    <%= f.file_field :photo_picture%>
     <%= f.label :photo_picture%><br />
  </div>

  <div class="links">
    <%= f.submit 'Submit', class: "btn btn-success btn-mini" %>
    <%= link_to 'View', user_album_photo_path, class:"btn btn-info btn-mini" %>
    <%= link_to 'Back to Album', user_album_path, class:"btn btn-inverse btn-mini" %>
  </div>
<% end %>