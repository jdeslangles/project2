<!DOCTYPE html>
<html>
<head>
  <title>Project2</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="container">
      <div class="header">
        <div class="row">

<!--           <div class="span9">
            <h1><img id="logo" src="../assets/Logo.png"></h1>
          </div>
          <div class="popup_login span3">
              < % if current_user %>
                <div class="logged-in">
                  < %=image_tag current_user.user_picture.thumb.url, class: "img-circle"%>
                  Logged in as < %= current_user.username %>
                </div>
              < % end %>
          </div>
        </div> -->

        <div class="navbar navbar-inverse">
          <nav class="navbar-inner">
            <ul class ="nav">
              <li class="active"><%= link_to("HOME", photo_wall_path) %>
              </li>

              <li>
                <%= link_to('Users', users_path)  %>
              </li>

              <% if current_user %>
                <li>
                  <%= link_to('My Profile', my_profile_path) %>
                </li>
              <% end %>

              <% unless current_user %>
                <li class="nav-signup"><%= link_to('Sign up', new_user_registration_path)  %>
                </li>

              <% end %>
              <% if current_user %>
                <li class="nav-user">Logged in as <%= current_user.username %>
                </li>
              <% end %>

              <li>
                <% if current_user %><%= link_to 'Logout', destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?' } %>
                <% else %>
                  <%= link_to 'Login', login_path %>
                <% end %>
              </li>
            </ul>
          </nav>
        </div>
      </div>
    </div>

  <% flash.each do |name,message| %>
    <div class="flash-message flash-message-<%=name%>">
      <%= message %>
    </div>
  <%end%>


<%= yield %>

  </div>
</body>
</html>
