<%= form_for([current_user,@album]) do |f| %>
  <% if @album.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@album.errors.count, "error") %> prohibited this album from being saved:</h2>

      <ul>
      <% @album.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %><br />
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %><br />
  </div>
  <div class="field">
    <%= f.label :privacy %>
    <%= f.check_box :privacy %><br />
  </div>
  <div class="links">
    <%= f.submit 'Submit', class: "btn btn-success btn-mini" %>
    <%= link_to 'Show', user_album_path(@album.user, @album), class:"btn btn-info btn-mini" %>
    <%= link_to 'Back', user_albums_path, class:"btn btn-inverse btn-mini" %>
  </div>
<% end %>